
const config = {
    user: 'user',
    password: 'password',
    database: 'database',
    host: 'host',
    port: 5432,
    statement_timeout: 50000,
    query_timeout: 50000,
    application_name: 'application_name'
};

export default config;
